{
    "name": "Flask Development",
    "dockerFile": "Dockerfile",
    "forwardPorts": [5000],
    "postCreateCommand": "pip install -r requirements.txt && flask db upgrade",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-python.vscode-pylance"
            ]
        }
    }
}